name: size-label
on: pull_request
jobs:
  size-label:
    runs-on: ubuntu-latest
    steps:
      - name: size-label
        uses: "codelytv/pr-size-labeler@v1"
        with:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
